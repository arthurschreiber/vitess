# Add your test case here for debugging and run go test -run=One.
"select id from (select id from user limit 10) u join (select * from (select user_id from user_extra limit 10) a) ue on u.id = ue.user_id"
"unsupported: filtering on results of cross-shard subquery"
{
}
